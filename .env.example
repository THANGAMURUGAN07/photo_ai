# PhotoFlow Environment Configuration
# Copy this file to .env and fill in your actual values

# ==================== DATABASE ====================
MONGODB_URI=mongodb://localhost:27017/photoflow

# ==================== EMAIL CONFIGURATION ====================
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_ENABLED=true

# ==================== SERVER CONFIGURATION ====================
PORT=5000
BASE_URL=http://localhost:5000
MAX_UPLOAD_MB=1024
PROCESS_TIMEOUT_MS=900000

# ==================== CLOUD STORAGE CONFIGURATION ====================
# Options: local, s3, mega, gcs
# Leave empty or set to 'local' to use local file storage
CLOUD_STORAGE_PROVIDER=local

# ==================== AWS S3 CONFIGURATION ====================
# Only required if CLOUD_STORAGE_PROVIDER=s3
AWS_S3_BUCKET=your-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# ==================== MEGA CONFIGURATION ====================
# Only required if CLOUD_STORAGE_PROVIDER=mega
MEGA_EMAIL=your-email@example.com
MEGA_PASSWORD=your-mega-password

# ==================== GOOGLE CLOUD STORAGE CONFIGURATION ====================
# Only required if CLOUD_STORAGE_PROVIDER=gcs
GCS_BUCKET=your-bucket-name
GCS_PROJECT_ID=your-project-id
GCS_KEY_FILENAME=./gcs-key.json

# ==================== NOTES ====================
# 1. For Gmail, use App Password, not your regular password
#    Generate at: https://myaccount.google.com/apppasswords
#
# 2. Choose only ONE cloud storage provider at a time
#
# 3. For production, set BASE_URL to your actual domain
#
# 4. Increase PROCESS_TIMEOUT_MS for large events (value in milliseconds)
#
# 5. MAX_UPLOAD_MB=0 means unlimited (use with caution)
